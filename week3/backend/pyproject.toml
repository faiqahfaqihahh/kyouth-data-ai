[project]
name = "backend"
version = "0.1.0"
description = "Backend service for processing prompt and PDF context"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "pydantic>=2.6.0",
    "python-dotenv>=1.0.1",
    "httpx>=0.27.0",
    "google-genai>=0.1.0",
]
