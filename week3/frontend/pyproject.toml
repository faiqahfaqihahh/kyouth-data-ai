[project]
name = "frontend"
version = "0.1.0"
description = "Frontend service for AI full-stack application"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.110.0",
    "jinja2>=3.1.3",
    "uvicorn>=0.28.0",
    "python-dotenv>=1.0.1",
]

[dependency-groups]
dev = [
    "ruff==0.15.0",
]